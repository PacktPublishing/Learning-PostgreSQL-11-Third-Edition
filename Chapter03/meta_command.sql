-- This should be executed with psql
\echo The "\echo" command is used to print messages
\echo To list the databses, "\l" can beused
\l
\echo "\c" meta command allows us to connnect to database
\echo We can not connect by defaul to template0 database
\c template0

